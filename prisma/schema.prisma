// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite" // локальная БД для разработки
  url      = env("DATABASE_URL")
}

/**
 * ==== Роли ====
 */
enum Role {
  STUDENT
  TEACHER
  ADMIN
}

/**
 * ==== Доп. enum'ы для LMS ====
 */
enum CourseVisibility {
  PRIVATE
  UNLISTED
  PUBLIC
}

enum EnrollmentStatus {
  ACTIVE
  COMPLETED
  CANCELLED
}

enum AssignmentType {
  ESSAY
  FILE
  QUIZ
}

enum SubmissionStatus {
  DRAFT
  SUBMITTED
  GRADED
}

enum QuizQuestionType {
  SINGLE
  MULTI
  TEXT
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  REFUNDED
}

/**
 * ===================== Пользователи и аутентификация =====================
 */

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime?
  image         String?
  passwordHash  String?

  role Role @default(STUDENT)

  // NextAuth
  accounts Account[]
  sessions Session[]

  // Профиль и семейные связи
  profile       Profile?
  childrenLinks ParentChild[] @relation("ParentLinksAsParent")
  parentLinks   ParentChild[] @relation("ParentLinksAsChild")

  // Учитель/Автор
  createdCourses Course[] @relation("CourseAuthor")

  // Ученик
  enrollments  Enrollment[]
  submissions  Submission[]
  quizAttempts QuizAttempt[]

  // Сообщения и чаты
  messages             Message[]
  conversationsCreated Conversation[] @relation("ConversationCreatedBy")

  // Платежи
  payments Payment[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @map("refresh_token")
  access_token      String? @map("access_token")
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @map("id_token")
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

/**
 * ===================== Профиль и семья =====================
 */

model Profile {
  id     String @id @default(cuid())
  user   User   @relation(fields: [userId], references: [id])
  userId String @unique

  locale   String? // "ru", "en", ...
  timezone String? // "Europe/Moscow", "Europe/Amsterdam", ...

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// Родитель ↔ Ребёнок (оба — User с разными ролями)
model ParentChild {
  id String @id @default(cuid())

  parent   User   @relation("ParentLinksAsParent", fields: [parentId], references: [id])
  parentId String

  child   User   @relation("ParentLinksAsChild", fields: [childId], references: [id])
  childId String

  createdAt DateTime @default(now())

  @@unique([parentId, childId])
}

/**
 * ===================== Курс → Разделы → Уроки =====================
 */

model Course {
  id          String           @id @default(cuid())
  slug        String           @unique
  title       String
  description String?
  language    String? // "ru", "en", ...
  level       String? // "Beginner", "A2", "C1", ...
  visibility  CourseVisibility @default(PRIVATE)

  priceCents Int?
  currency   String? // "RUB", "USD", "KZT", ...

  author   User   @relation("CourseAuthor", fields: [authorId], references: [id])
  authorId String

  sections      Section[]
  enrollments   Enrollment[]
  conversations Conversation[]
  payments      Payment[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Section {
  id       String @id @default(cuid())
  course   Course @relation(fields: [courseId], references: [id])
  courseId String

  title String
  order Int    @default(0)

  lessons Lesson[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([courseId, order])
}

model Lesson {
  id        String  @id @default(cuid())
  section   Section @relation(fields: [sectionId], references: [id])
  sectionId String

  title String
  slug  String @unique
  order Int    @default(0)

  contentJson Json? // можно хранить MD/JSON
  videoUrl    String?
  durationSec Int?
  isFree      Boolean @default(false)

  quiz        Quiz?
  assignments Assignment[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([sectionId, order])
}

/**
 * ===================== Записи на курс и прогресс =====================
 */

model Enrollment {
  id       String @id @default(cuid())
  user     User   @relation(fields: [userId], references: [id])
  userId   String
  course   Course @relation(fields: [courseId], references: [id])
  courseId String

  status   EnrollmentStatus @default(ACTIVE)
  progress Int              @default(0) // 0..100

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([userId, courseId])
}

/**
 * ===================== Домашние задания и ответы =====================
 */

model Assignment {
  id       String @id @default(cuid())
  lesson   Lesson @relation(fields: [lessonId], references: [id])
  lessonId String

  title String
  type  AssignmentType
  dueAt DateTime?

  submissions Submission[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Submission {
  id           String     @id @default(cuid())
  assignment   Assignment @relation(fields: [assignmentId], references: [id])
  assignmentId String

  author   User   @relation(fields: [authorId], references: [id])
  authorId String

  text     String?
  fileUrl  String?
  grade    Float?
  feedback String?
  status   SubmissionStatus @default(DRAFT)

  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([assignmentId, authorId])
}

/**
 * ===================== Квизы =====================
 */

model Quiz {
  id       String @id @default(cuid())
  lesson   Lesson @relation(fields: [lessonId], references: [id])
  lessonId String @unique

  title     String
  questions QuizQuestion[]
  attempts  QuizAttempt[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model QuizQuestion {
  id     String @id @default(cuid())
  quiz   Quiz   @relation(fields: [quizId], references: [id])
  quizId String

  order         Int              @default(0)
  type          QuizQuestionType
  prompt        String
  answerKeyText String? // для TEXT

  choices QuizChoice[]

  @@index([quizId, order])
}

model QuizChoice {
  id         String       @id @default(cuid())
  question   QuizQuestion @relation(fields: [questionId], references: [id])
  questionId String

  text      String
  isCorrect Boolean @default(false)
}

model QuizAttempt {
  id     String @id @default(cuid())
  quiz   Quiz   @relation(fields: [quizId], references: [id])
  quizId String

  user   User   @relation(fields: [userId], references: [id])
  userId String

  score      Float?
  startedAt  DateTime  @default(now())
  finishedAt DateTime?

  @@index([quizId, userId])
}

/**
 * ===================== Чаты по курсам =====================
 */

model Conversation {
  id       String  @id @default(cuid())
  course   Course? @relation(fields: [courseId], references: [id])
  courseId String?

  createdBy   User   @relation("ConversationCreatedBy", fields: [createdById], references: [id])
  createdById String

  messages Message[]

  createdAt DateTime @default(now())
}

model Message {
  id             String       @id @default(cuid())
  conversation   Conversation @relation(fields: [conversationId], references: [id])
  conversationId String

  sender   User   @relation(fields: [senderId], references: [id])
  senderId String

  content   String
  createdAt DateTime @default(now())
}

/**
 * ===================== Платежи =====================
 */

model Payment {
  id     String @id @default(cuid())
  user   User   @relation(fields: [userId], references: [id])
  userId String

  course   Course @relation(fields: [courseId], references: [id])
  courseId String

  amountCents Int
  currency    String
  provider    String // "Stripe" | "YooKassa" | "PayPal" | ...
  status      PaymentStatus @default(PENDING)
  externalId  String? // ID транзакции у провайдера

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId, courseId])
}
